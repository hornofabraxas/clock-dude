name: release

# On a version tag push (vX.Y), verify the tag matches package.json, run the
# same build gate as ci.yml, and publish a GitHub Release with the .pbw
# attached (named clock-dude-vX.Y.pbw).
#
# The build steps live in the .github/actions/pebble-build composite action,
# shared with ci.yml.

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Verify tag matches package.json version
        run: |
          set -euo pipefail
          TAG="${GITHUB_REF_NAME#v}"
          PKG="$(jq -r .version package.json)"
          echo "tag=$TAG package.json=$PKG"
          if [[ "$TAG" != "$PKG" ]]; then
            echo "::error::tag v$TAG does not match package.json version $PKG" >&2
            exit 1
          fi

      - name: Build Clock Dude
        uses: ./.github/actions/pebble-build

      - name: Stage .pbw
        run: |
          set -euo pipefail
          cp build/clock-dude.pbw "clock-dude-${GITHUB_REF_NAME}.pbw"

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          gh release create "${GITHUB_REF_NAME}" \
            "clock-dude-${GITHUB_REF_NAME}.pbw" \
            --title "${GITHUB_REF_NAME}" \
            --notes "Automated build for ${GITHUB_REF_NAME}. See CHANGELOG.md for details." \
            --verify-tag
